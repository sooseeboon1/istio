apiVersion: v1
kind: Service
metadata:
  name: {{ include "aaclient.fullname" . }}
  labels:
    {{- include "aaclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aaclient.selectorLabels" . | nindent 4 }}
